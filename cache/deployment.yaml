apiVersion: apps/v1
kind: Deployment
metadata:
  name: cache
  labels:
    name: cache
spec:
  replicas: 1
  selector:
    matchLabels:
      name: cache
  template:
    metadata:
      labels:
        name: cache
    spec:
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: cache
      containers:
        - name: redis
          image: docker.io/redis:7.4-alpine
          ports:
            - containerPort: 6379
              name: default
              protocol: TCP
          resources:
            requests:
              cpu: 50m
              memory: 100Mi
            limits:
              cpu: 100m
              memory: 200Mi
          volumeMounts:
            - mountPath: /data
              name: data